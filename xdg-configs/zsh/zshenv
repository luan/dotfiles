# Zsh environment — sourced for every shell (interactive, login, scripts).
# Keep this idempotent and fast; heavy work belongs in .zshrc.
# Do not set ZDOTDIR here: ~/.zshrc is the local entrypoint and sources the
# repo-managed ~/.config/zsh/zshrc explicitly.

export XDG_CONFIG_HOME="$HOME/.config"

export HOMEBREW_PREFIX="/opt/homebrew"
export HOMEBREW_CELLAR="/opt/homebrew/Cellar"
export HOMEBREW_REPOSITORY="/opt/homebrew/Homebrew"
export INFOPATH="/opt/homebrew/share/info:${INFOPATH:-}"

export ZEROBREW_BIN="${ZEROBREW_BIN:-$HOME/.local/bin}"
export ZEROBREW_ROOT="${ZEROBREW_ROOT:-/opt/zerobrew}"
export ZEROBREW_PREFIX="${ZEROBREW_PREFIX:-/opt/zerobrew}"

export EDITOR=nvim
export GIT_EDITOR=nvim

export BUN_INSTALL="$HOME/.bun"
export VCPKG_ROOT="$HOME/vcpkg"
export CARAPACE_BRIDGES='zsh,fish,bash,inshellisense'

typeset -U path
path=("$HOME/.local/share/mise/shims" "$HOME/bin" "$ZEROBREW_BIN" "$ZEROBREW_PREFIX/bin" "/opt/homebrew/bin" "/opt/homebrew/sbin" "$HOME/.local/bin" "$BUN_INSTALL/bin" "$HOME/.cargo/bin" $path)

if [[ -d "$ZEROBREW_PREFIX/lib/pkgconfig" ]]; then
  export PKG_CONFIG_PATH="$ZEROBREW_PREFIX/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
fi

# Slack MCP OAuth client registers this localhost redirect port.
export MCP_OAUTH_CALLBACK_PORT=3118
