[package]
name = "tmux-session"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = "^0"
crossterm = "^0"
nucleo-matcher = "^0"
serde = { version = "^1", features = ["derive"] }
serde_json = "^1"
chrono = "^0"

[profile.release]
opt-level = "z"
lto = true
strip = true
